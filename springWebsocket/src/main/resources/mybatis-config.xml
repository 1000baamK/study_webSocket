<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
  
<configuration>
	
	
	<settings>
		<setting name="jdbcTypeForNull" value="NULL"/>
	</settings>  
  	
  	<typeAliases>
  	</typeAliases>
  	
  	<!-- 
  		environment,dataSource에 기술했던 DB연결정보 관련해서는 root-context.xml에 작성한다.
  		-작성이유 : DB등록은 서버연동과 동시에 이루어져야하기 때문에
  				root-context는 서버가 동작됨가 동시에 읽혀진다.
  	
  	 -->
  	
  	
  		<!-- mapper 등록시 이미 존재하는 mapper파일만 등록 가능 (namespace도 등록되어야함) -->
  
		<!-- <mappers>
  	 		<mapper resource="/mappers/test-mapper.xml"/>
  		</mappers> --> 
  
  
</configuration>
  